<Window x:Class="EncryptionDiary.Client.Windows.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Password" Height="150" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtMessage" Text="Enter your password:"/>
        <PasswordBox Grid.Row="1" x:Name="txtPassword" Margin="0,10"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="0,0,10,0" Click="btnOk_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" Click="btnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>